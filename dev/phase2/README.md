# Phase 2 Documentation

The former `phase 2 contract info` folder has been retired.

Use:

- `PHASE_2_CAMPAIGN_CONCEPT_CONTRACT.md` for the human-readable generation and validation contract;
- `PHASE_2_MANUAL_ROUND_TRIP.md` for operator workflow;
- `PHASE_2_PDF_EXPORT.md` for document export.

The authoritative JSON Schema belongs in:

```text
src/ai/phase2/campaignConcept.schema.json
```

The sanitized JSON fixture belongs in:

```text
scripts/fixtures/phase2/campaignConcept.example.json
```

The prose concept example belongs in:

```text
examples/phase2/the-ember-beneath-concept-example.md
```
